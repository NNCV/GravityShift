using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;
using System.IO;
using System.Runtime.Serialization.Formatters.Binary;

public static class GlobalManager {

    /*
    public void LoadFile()
    {

        file.Close();

        currentPEM.currentHull = pss.savedPEM.currentHull;
        currentPEM.currentBlasters = pss.savedPEM.currentBlasters;
        currentPEM.pmm = pss.savedPMM;

        currentHull = savefiles[selectedSavefile].savedPEM.currentHull;
        for (int i = 0; i < savefiles[selectedSavefile].savedPEM.currentHull.maxWeaponNumber; i++)
        {
            if (savefiles[selectedSavefile].savedPEM.currentBlasters[i] == null)
                Debug.Log("haha no blaster at" + i);
            else
            {
                currentBlasters[i] = savefiles[selectedSavefile].savedPEM.currentBlasters[i];
            }
        }
    }
    */
}
